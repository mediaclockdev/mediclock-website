/* ============================================================
   package.js
   SEO Packages page only.
   Media Clock — static rebuild
   All core interactions (hero, contact panel, FAQs) are handled in main.js.
   ============================================================ */
